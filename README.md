# Epilogue

<img src="logo.svg" width="96" align="right" />

**files, remembered.** 个人文件 AI 工作流桌面应用（macOS / Windows / Linux）。

[English](README.en.md) | 中文

开源（**ALE 1.1 & GPL-3.0**），**不收集任何信息**——配置、索引、向量全部留在本机；音视频转写默认本机完成，音频不出设备。

[**下载发布包 →**](https://github.com/wuyilingwei/epilogue/releases)（全平台双架构：macOS / Windows / Linux × x64 / arm64）

## 功能

- **清理**：定期扫描长期未动的过期文件，AI 按你的自然语言习惯给出归位建议，逐项确认后移动；理解成套内容（课件系列、便携软件、剧集）并整体归档，能利用目标文件夹的多层结构。
- **寻物**：「上学期的实验报告放哪了？」一句话找回文件——关键字 / 相似度 / AI 问答三种模式，支持用文字描述找图片、理解「去年」「上周」等相对时间。
- **助手**：内置对话智能体——改设置、把「以后发票都放 财务/」记成习惯、查文件。
- **可选自动化**（默认关闭，仅可手动开启）：Solo 模式定时自动归类免审批；允许 AI 将无价值临时文件移入系统回收站。

## AI 能力与多 Provider 灾备

每类能力都是一个**灾备列表**（拖动排序、行级启用、一键连通测试），从上到下依次尝试：

| 能力 | 内置预设 | 说明 |
| --- | --- | --- |
| Chat / 对话模型 | Pollinations（免 Key）→ OpenRouter Free → OpenCode Zen Free | 注册填 Key 即激活后两者；可加任意 OpenAI-compatible 服务 |
| Embeddings / 文本向量 | **本机 BGE 中文**（离线） | 可追加云端 API 灾备 |
| Image Embedding / 图形向量 | **本机 CLIP 系四方案可选**（默认中文优化，按需下载） | 用「红色的海报」这类描述找图 |
| Whisper / 转写 | **本机两档**（低 / 高精度，独立下载启用） | 可加云端转写灾备 |

支持识别压缩包、Office、PDF、纯文本、图片与音视频。

## 轻量驻留

经过深度优化，占用极低：托盘常驻时几乎不占资源，界面与数据只在需要时加载，重负载任务自动让路给你的前台工作，电池供电时自动放缓。托盘态不占 Dock（macOS）。

## 运行与开发

```bash
npm install
npm start      # 启动应用
npm test       # 单元测试
bash scripts/pack.sh [platform] [arch]   # 本地打包
```

推送 `v*` 标签即由 CI 自动构建全平台双架构发布包。

## 隐私与许可

- 无遥测、无统计、无回传；所有数据保存在本机，可在应用内查看与清理。
- 使用 AI 功能时，相关文件内容会发送至**你配置（或内置默认）的服务商**，受其各自隐私政策约束；高度敏感文件请勿使用 AI 功能。
- 许可：**ALE 1.1（反劳动剥削许可证，优先适用）& GPL-3.0**——GPL 权利以 ALE 合规为条件，ALE 的约束主要作用于商业与用工实体。条款与许可证全文均内置于应用（设置 → 关于）。
